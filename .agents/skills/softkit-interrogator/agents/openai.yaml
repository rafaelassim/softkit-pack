interface:
  display_name: "SoftKit Interrogator"
  short_description: "Clarify requirements for an approved work item."
  default_prompt: "Execute the SoftKit Interrogator stage for the active approved SoftKit work item and return the required handoff."

policy:
  allow_implicit_invocation: false
