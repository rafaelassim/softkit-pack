interface:
  display_name: "SoftKit Reviewer"
  short_description: "Independently review approved changes."
  default_prompt: "Execute the SoftKit Reviewer stage for the active approved SoftKit work item and return the required handoff."

policy:
  allow_implicit_invocation: false
